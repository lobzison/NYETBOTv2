nyetbot {
  ollama {
    context {
      dossier {
        enabled           = true
        summary-max-chars = 500

        model-config = ${nyetbot.ollama.model-defaults}

        model-config {
          num-predict = 256
        }
      }
    }
  }
}
